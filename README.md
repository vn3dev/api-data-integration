npm init -y

npm install --save-dev typescript ts-node @types/node

npx tsc --init

npx ts-node src/index.ts

```
feat → nova funcionalidade
fix → correção de bug
chore → configuração/infra do projeto
docs → documentação
refactor → refatoração`
```